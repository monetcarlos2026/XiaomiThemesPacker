# ADB

Run `download_adb.bat` from the project root to install Google Platform Tools files here:

- `adb.exe`
- `AdbWinApi.dll`
- `AdbWinUsbApi.dll`

MiTheme Builder first checks this directory, then falls back to `adb` from `PATH`.
